# metro.kennysoft.kr 설계

수도권 전철 이용에 필요한 정보를 한 화면에 모으는 개인용 웹페이지. 공개 사이트로 운영한다.

지금까지는 필요한 정보가 앱과 홈페이지 다섯 군데에 흩어져 있었다. 실시간 위치는 또타지하철 앱, 용인경전철은 그 홈페이지, 빠른 환승 문 위치는 나무위키, 신분당선 시간표는 신분당선 홈페이지, 수인분당선 행선지는 코레일 홈페이지를 각각 열어야 했다. 이 페이지는 그것을 한 곳에서 본다.

- 사용자: 1명 기준으로 설계하되 공개 운영
- 주 이용 노선: 신분당선
- 관련 프로젝트: [`ledger-memo`](../ledger-memo) (인프라와 배포 구성을 그대로 따른다)

## 1. 설계 원칙

### 1.1 열차를 놓치지 않게 하는 것이 목적

이 페이지가 답해야 할 질문은 "다음 열차가 언제 오는가" 가 아니라 **"내가 지금 타야 할 열차가 무엇인가"** 다. 신분당선에는 광교까지 가지 않는 정자행이 섞여 있고, 수인분당선에는 죽전행과 인천행이 섞여 있다. 시각만 알려주고 행선지를 빠뜨리면 이 페이지는 쓸모가 없다.

행선지 정보는 두 경로 모두에 들어 있다. 시각표 PDF 는 `54(정자)` 처럼 괄호로 표기하고, 실시간 API 는 `trainLineNm` 에 `용산행 - 신촌(경의중앙선)방면` 형태로 담는다.

### 1.2 하루 1,000회 안에서 돈다

서울시 실시간 지하철 API 는 인증키당 **하루 1,000회** 제한이 있다. 운행시간 19시간을 이 예산으로 나누면 70초에 1회다. 따라서 **브라우저가 API 를 직접 부르는 구조는 성립하지 않는다.** 서버가 한 번 받아 캐시하고 모든 방문자가 그 캐시를 공유해야 한다. 서버를 두는 이유가 키를 숨기기 위해서가 아니라 호출 예산 때문이다.

활용사례 갤러리에 인증키와 함께 콘텐츠를 등록하면 이 제약이 풀린다. 화면을 완성하고 배포한 뒤 등록한다. 등록 전까지는 개발 중 호출도 예산에 포함된다.

### 1.3 저장소를 두지 않는다

쓰기가 없고 개인 데이터도 없다. 보관할 것은 파싱된 시각표와 환승 정보뿐이고 둘 다 갱신이 드문 정적 데이터라, 기동 시 읽어 메모리에 두면 충분하다. DB 를 빼면 배포와 백업이 사라진다. 방문자별 설정(자주 쓰는 역 등)이 필요해지면 브라우저 localStorage 로 처리한다.

## 2. 데이터 소스

### 2.1 신분당선 시각표

공식 홈페이지가 전체 시각표를 ZIP 으로 제공한다. 평일판과 공휴일판 PDF 두 개가 들어 있고, 각 PDF 는 역당 1페이지씩 16페이지다.

```
https://www.shinbundang.co.kr/util/download.jsp?fPath=/files/notice/&fName=all_kor.zip
```

`pdftotext -layout -enc UTF-8` 로 추출하면 아래 형태가 나온다. 좌우 두 열이 각각 상행과 하행이고, 가운데 열이 시(hour)다.

```
신사 열차시각표
기본계획번호 SBL4017R2  적용기간 : 2025-09-02~9999-12-31  운행요일 : 평일
                    시            광교방면
                    09  03 07 12 17 22 27 31 36 42 49 54(정자) 59
                    23  07 18(정자) 24 35 49
```

개정 감지는 파일 해시가 아니라 **`기본계획번호` 와 `적용기간`** 으로 한다. 재출력만 해도 해시는 바뀌지만 이 두 값은 그대로다.

역 선택 조회(`POST /index.jsp` 에 `st_cd=SB001`~`SB016`)도 있으나 응답이 EUC-KR HTML 이고 세션에 의존하는 것으로 보여, PDF 쪽을 정본으로 쓴다.

### 2.2 실시간 도착과 위치

서울시 열린데이터광장의 실시간 지하철 API 를 쓴다. 인증키는 일반 인증키와 별도로 **실시간 지하철 전용 키**를 신청해야 한다.

```
http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/{SERVICE}/{START}/{END}/{PARAM}
```

| 서비스 | PARAM | 용도 |
|---|---|---|
| `realtimeStationArrival` | 역명 | 그 역의 도착 예정 열차. 행선지와 종착역 포함 |
| `realtimePosition` | 노선명 | 그 노선 전체 열차의 현재 위치 |

쓸 만한 응답 필드는 `trainLineNm`(행선지와 방면), `bstatnNm`(종착역), `arvlMsg2`(도착 안내 문구), `subwayId`(노선), `recptnDt`(수신 시각), `lstcarAt`(막차 여부)다.

서울교통공사 관할 밖 노선도 들어온다. 서울역 조회 결과에 1호선(1001), 4호선(1004)과 함께 경의중앙선(1063), 공항철도(1065)가 나왔다.

**주의할 함정이 있다.** 데이터가 없을 때와 존재하지 않는 노선명을 넣었을 때의 응답이 `INFO-200 해당하는 데이터가 없습니다` 로 완전히 같다. 그래서 운행하지 않는 시간대에는 "이 노선을 지원하지 않는다" 와 "지금 열차가 없다" 를 구분할 수 없다. 노선 지원 여부는 반드시 **운행 시간대에** 판정해야 한다.

### 2.3 환승 문 위치

국가철도공단이 운영기관별로 파일데이터를 낸다. 로그인 없이 받을 수 있다. 나무위키는 비영리 조건이 붙은 라이선스라 공개 사이트에 쓸 수 없고, 이 공식 데이터가 같은 정보를 담는다.

신분당선판(공공데이터포털 15041072)은 12행이고 cp949 CSV 다.

```
철도운영기관명,노선명,역명,환승철도운영기관,환승선명,환승이후역명,환승기점역명,차량순서,차량출입문번호
네오트랜스주식회사,신분당선,강남,서울교통공사,2호선,교대(법원.검찰청),충정로(경기대입구),1,1
네오트랜스주식회사,신분당선,정자,코레일,수인분당,미금(분당서울대병원),왕십리,2,4
```

`환승기점역명` 이 방향을 가르는 키다. 같은 역에서 갈아타도 어느 방향으로 가느냐에 따라 내릴 문이 다르다.

수록 범위가 **네오트랜스 관할 구간(강남~정자)뿐**이다. 신사, 논현, 신논현, 미금이 빠져 있다. 나머지 구간 운영사의 데이터가 따로 있는지 확인이 필요하다.

다운로드 URL 의 `atchFileId` 는 데이터 갱신 시 바뀌므로 하드코딩하지 않는다.

### 2.4 용인경전철

공식 실시간 페이지가 쓰는 엔드포인트를 그대로 쓴다.

```
https://everlinecu.com/api/api009.json
```

응답은 `data[]` 배열이고 항목마다 `StCode`(정거장), `TrainNo`(열차번호), `StatusCode`(상태), `updownCode`(상하행), `time` 을 담는다. 인증이 없고 호출 제한도 공지되어 있지 않다.

## 3. 호출 예산

활용사례 등록 전까지는 아래를 지킨다.

| 항목 | 값 |
|---|---|
| 일일 한도 | 1,000회 |
| 1회 최대 반환 | 1,000건 |
| 운행시간 19시간 기준 최소 캐시 주기 | 70초 |

역 단위로 조회하면 역 개수만큼 곱해지므로, 노선 단위 조회(`realtimePosition`)로 한 번 받아 서버에서 역별로 쪼개는 쪽이 예산에 유리하다.

## 4. 스택

ledger-memo 구성을 따르되 저장소 계층을 뺀다.

| 구분 | 사용 |
|---|---|
| 백엔드 | Kotlin, Spring Boot |
| 저장소 | 없음 (기동 시 파싱해 메모리 보관) |
| 런타임 | GraalVM native image (aarch64) |
| 프론트 | 빌드 없는 단일 HTML, vanilla JS, PWA |
| 배포 | GitHub Actions (ARM64 runner), GHCR, Oracle Cloud A1, Podman |
| 앞단 | Apache httpd 리버스 프록시 |

시각표 PDF 파싱에 `pdftotext` 가 필요하다. 런타임 이미지에 poppler 를 넣을지, 빌드 시점에 파싱해 JSON 을 산출물에 포함할지는 정하지 않았다. 후자가 런타임 의존을 없애지만 시각표 개정 때마다 재배포가 필요하다.

## 5. 확인 예정

운행 시간대에 확인해야 판정되는 항목이다.

1. **신분당선이 `realtimePosition` 에 포함되는가.** 이 답에 따라 화면 구성이 갈린다. 포함되면 실시간 위치가 주가 되고, 빠지면 시각표 기준으로 "지금 시각의 다음 열차" 를 계산하는 쪽이 주가 된다.
2. 수인분당선과 용인경전철의 실시간 커버리지.
3. 신사~강남, 정자~광교 구간의 환승 데이터가 별도 데이터셋으로 존재하는가.
4. 용인경전철 `api009.json` 의 운행 시간대 응답 형태. 운행 종료 후에는 `data` 가 빈 배열이다.

## 6. 스코프 밖

- 경로 탐색과 소요시간 계산. 기존 지도 앱이 충분히 잘한다.
- 운임 계산. 신분당선 별도 운임이 있으나 교통카드가 알아서 한다.
- 버스 연계.
- 로그인과 개인화 서버 저장.
