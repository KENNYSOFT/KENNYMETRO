# metro.kennysoft.kr 설계

수도권 전철 이용에 필요한 정보를 한 화면에 모으는 개인용 웹페이지. 공개 사이트로 운영한다.

지금까지는 필요한 정보가 앱과 홈페이지 다섯 군데에 흩어져 있었다. 실시간 위치는 또타지하철 앱, 용인경전철은 그 홈페이지, 빠른 환승 문 위치는 나무위키, 신분당선 시간표는 신분당선 홈페이지, 수인분당선 행선지는 코레일 홈페이지를 각각 열어야 했다. 이 페이지는 그것을 한 곳에서 본다.

- 사용자: 1명 기준으로 설계하되 공개 운영
- 주 이용 노선: 신분당선
- 관련 프로젝트: [`ledger-memo`](../ledger-memo) (인프라와 배포 구성을 그대로 따른다)

## 1. 설계 원칙

### 1.1 열차를 놓치지 않게 하는 것이 목적

이 페이지가 답해야 할 질문은 "다음 열차가 언제 오는가" 가 아니라 **"내가 지금 타야 할 열차가 무엇인가"** 다. 신분당선에는 광교까지 가지 않는 정자행이 섞여 있고, 수인분당선에는 죽전행과 인천행이 섞여 있다. 시각만 알려주고 행선지를 빠뜨리면 이 페이지는 쓸모가 없다.

행선지 정보는 두 경로 모두에 들어 있다. 시각표 PDF 는 `54(정자)` 처럼 괄호로 표기하고, 실시간 API 는 `trainLineNm` 에 `용산행 - 신촌(경의중앙선)방면` 형태로 담는다.

### 1.2 하루 1,000회 안에서 돈다

서울시 실시간 지하철 API 는 인증키당 **하루 1,000회** 제한이 있다. 운행시간 19시간을 이 예산으로 나누면 70초에 1회다. 따라서 **브라우저가 API 를 직접 부르는 구조는 성립하지 않는다.** 서버가 한 번 받아 캐시하고 모든 방문자가 그 캐시를 공유해야 한다. 서버를 두는 이유가 키를 숨기기 위해서가 아니라 호출 예산 때문이다.

활용사례 갤러리에 인증키와 함께 콘텐츠를 등록하면 이 제약이 풀린다. 화면을 완성하고 배포한 뒤 등록한다. 등록 전까지는 개발 중 호출도 예산에 포함된다.

### 1.3 저장소를 두지 않는다

쓰기가 없고 개인 데이터도 없다. 보관할 것은 파싱된 시각표와 환승 정보뿐이고 둘 다 갱신이 드문 정적 데이터라, 기동 시 읽어 메모리에 두면 충분하다. DB 를 빼면 배포와 백업이 사라진다. 방문자별 설정(자주 쓰는 역 등)이 필요해지면 브라우저 localStorage 로 처리한다.

**예외가 하나 있다. 그 날 나간 API 호출 수는 파일에 남긴다.** 일일 한도는 자정에 돌아오는데, 메모리에만 두면 재배포할 때마다 0 부터 다시 세어 화면의 남은 예산이 실제 한도와 어긋난다. 배포는 그 날 여러 번 일어나기도 한다. 그래서 날짜별 한 줄짜리 텍스트 파일 하나를 컨테이너 밖 볼륨에 두고, 기록에 실패해도 호출은 계속한다 - 예산 표시가 못 미더운 것과 화면이 안 뜨는 것 중에는 앞이 낫다. DB 를 들이지 않으려는 것이 이 원칙의 목적이므로 파일 한 개로 끝낸다.

### 1.4 노선 전체가 아니라 관심 구간을 본다

노선을 다 그리면 수인분당선만 62역이고 다섯 노선을 합치면 175역이다. 그중 실제로 타는 구간은 일부이고, 전부 그리면 정작 볼 곳을 스크롤로 찾아야 한다.

그래서 노선마다 **관심 구간**과 그 안의 **주요 역**을 정해 기본으로 그 구간만 그린다. 전 구간은 버튼 하나로 펼친다. 구간 밖에 있는 열차는 세어서 "구간 밖 N대" 로만 알린다 - 안 보이는 것과 없는 것을 가르기 위한 것이다.

지금 값은 `lines.yml` 에 고정돼 있다. 사람마다 다른 값이므로 나중에 옮길 자리이고, 브라우저가 이미 **펼친 노선과 전 구간 보기 여부를 localStorage 에 기억한다**. 관심 구간도 같은 자리로 가면 된다 - 서버에 계정을 두지 않는다는 1.3 과 어긋나지 않는다.

## 2. 데이터 소스

### 2.1 신분당선 시각표

공식 홈페이지가 전체 시각표를 ZIP 으로 제공한다. 평일판과 공휴일판 PDF 두 개가 들어 있고, 각 PDF 는 역당 1페이지씩 16페이지다.

```
https://www.shinbundang.co.kr/util/download.jsp?fPath=/files/notice/&fName=all_kor.zip
```

`pdftotext -layout -enc UTF-8` 로 추출하면 아래 형태가 나온다. 좌우 두 열이 각각 상행과 하행이고, 가운데 열이 시(hour)다.

```
신사 열차시각표
기본계획번호 SBL4017R2  적용기간 : 2025-09-02~9999-12-31  운행요일 : 평일
                    시            광교방면
                    09  03 07 12 17 22 27 31 36 42 49 54(정자) 59
                    23  07 18(정자) 24 35 49
```

개정 감지는 파일 해시가 아니라 **`기본계획번호` 와 `적용기간`** 으로 한다. 재출력만 해도 해시는 바뀌지만 이 두 값은 그대로다.

**상행과 하행은 컬럼 위치로 갈라야 한다.** 줄 앞에서부터 정규식으로 숫자를 집으면 상행 열의 분(minute)을 시(hour)로 읽는다. 실제로 그렇게 집계했더니 신분당선이 운행하지 않는 새벽 2시대와 4시대에 정자행이 있는 것으로 나왔다. 신사와 광교는 시종착역이라 한쪽 열만 있어 이 실수가 드러나지 않으므로, 파서 검증은 반드시 중간역으로 한다.

역 선택 조회(`POST /index.jsp` 에 `st_cd=SB001`~`SB016`)도 있으나 응답이 EUC-KR HTML 이고 세션에 의존하는 것으로 보여, PDF 쪽을 정본으로 쓴다.

### 2.2 실시간 도착과 위치

서울시 열린데이터광장의 실시간 지하철 API 를 쓴다. 인증키는 일반 인증키와 별도로 **실시간 지하철 전용 키**를 신청해야 한다.

```
http://swopenapi.seoul.go.kr/api/subway/{KEY}/json/{SERVICE}/{START}/{END}/{PARAM}
```

| 서비스 | PARAM | 용도 |
|---|---|---|
| `realtimeStationArrival` | 역명 | 그 역의 도착 예정 열차. 행선지와 종착역 포함 |
| `realtimePosition` | 노선명 | 그 노선 전체 열차의 현재 위치 |

`realtimeStationArrival` 에서 쓸 만한 필드는 `trainLineNm`(행선지와 방면), `bstatnNm`(종착역), `arvlMsg2`(도착 안내 문구), `subwayId`(노선), `recptnDt`(수신 시각), `lstcarAt`(막차 여부)다. `realtimePosition` 은 `trainNo`, `statnNm`(현재역), `statnTnm`(종착역), `updnLine`(0 상행 / 1 하행), `trainSttus`, `directAt`(급행), `lstcarAt` 을 준다.

**화면에 올릴 노선이 모두 들어온다.** 2026-09-22 월요일 23:15 에 확인한 결과다.

| 노선 | 열차 | 표본 |
|---|---|---|
| 신분당선 | 11 | 4번 열차, 신사, 정자행 |
| 2호선 | 31 | 2495번 열차, 문래, 성수종착 |
| 9호선 | 29 | 9234번 열차, 언주, 중앙보훈병원행 |
| 수인분당선 | 26 | 6249번 열차, 왕십리, 죽전행 |

**`trainNo` 의 성격이 노선마다 다르다.** 신분당선만 1~2자리이고 나머지는 4자리다. 신분당선의 값은 **편성번호**다 - 2분 간격으로 네 번 추적했더니 신사와 광교에서 회차하는 순간에도 번호가 유지됐고, 관측된 값이 전부 23개 편성 범위 안에 들어왔다.

```
18: 신사>신사 | 신사>광교 | 신사>광교 | 논현>광교
21: 광교중앙>광교 | 광교>광교 | 광교>신사
```

운행번호였다면 회차하며 바뀐다. 그래서 신분당선에서만 열차가 몇 차분 차량인지 알 수 있고, 다른 노선은 어느 차량인지 특정할 수 없어 차수를 매기지 않는다.

**`trainSttus` 는 0, 1, 2 만 나온다.** 진입, 도착, 출발이다. 지금은 역 단위로만 그리므로 쓰지 않지만, 역과 역 사이에 열차를 놓으려면 이 값이 근거가 된다. **`directAt` 은 9호선에서만 1 이 나온다** - 그 노선에 급행이 있어서다. 같은 조회에서 29대 중 12대가 급행이었다.

**`statnTnm` 이 행선지 구분의 열쇠다.** 시각표의 `54(정자)` 괄호 표기와 같은 정보를 실시간으로 얻는다.

**다만 종착 처리 중인 열차가 목록에 섞여 있어, 종착역을 그대로 세면 안 된다.** 10:22 조회에서 종착역 집계가 광교 6, 신사 8, 정자 1 로 나와 정자행이 운행 중인 것처럼 보였다. 그 정자행은 현재역도 정자였다. 10분 뒤 재조회하니 정자행은 0대이고 그 열차는 목록에서 사라졌다. 운행 중인 정자행이 아니라 정자에 도착해 운행을 마치던 열차였다.

시각표와도 맞는다. 신사역 기준 정자행은 09:54, 23:18, 00:10, 00:22 네 편뿐이고, 09:54 신사 발이 약 28분 뒤 정자에 닿는다. 10:22 조회는 그 열차가 막 도착한 순간을 잡은 것이다.

따라서 **`statnNm == statnTnm` 인 열차는 "지금 탈 수 있는 열차" 에서 제외한다.** 종착역 부근의 상태 전이는 이렇게 이어진다.

1. 하행 열차가 광교 도착 (현재 광교, 종착 광교, 하행)
2. 회차 중 (현재 광교, 종착 신사, **하행**) — 종착역과 진행 방향이 어긋나 보이는 구간
3. 상행 출발 준비 (현재 광교, 종착 신사, 상행)

2번 상태의 열차를 `updnLine` 만 보고 그리면 방향이 반대로 나간다. 정자행처럼 중간역에서 끝나는 열차는 도착 후 목록에서 아예 빠진다.

**주의할 함정이 있다.** 데이터가 없을 때와 존재하지 않는 노선명을 넣었을 때의 응답이 `INFO-200 해당하는 데이터가 없습니다` 로 완전히 같다. 그래서 운행하지 않는 시간대에는 "이 노선을 지원하지 않는다" 와 "지금 열차가 없다" 를 구분할 수 없다. 노선을 추가할 때는 반드시 **운행 시간대에** `scripts/probe-coverage.sh` 로 판정한다.

**이 성질 때문에 잘못된 요청도 빈 화면으로 끝난다.** 노선명을 손수 URL 인코딩해 문자열로 이어 붙이면 안 된다. Spring 의 `RestClient.uri(String)` 은 그 문자열을 URI 템플릿으로 해석해 이미 인코딩된 `%` 를 `%25` 로 한 번 더 인코딩하고, 서울시 API 는 깨진 노선명을 400 이 아니라 `INFO-200` 으로 받아넘긴다. 로그에 아무것도 남지 않은 채 열차 0대가 된다. 노선명은 템플릿 변수로 넘겨 인코딩을 한 번만 거치게 한다.

### 2.3 환승 문 위치

나무위키 "수도권 전철 환승 정보" 를 쓴다. **지금 탄 열차가 어느 쪽으로 가는지로 문이 갈린다.** 국가철도공단도 같은 성격의 파일데이터를 내지만 쓰지 않는다 (2.3.1). 라이선스 처리는 2.3.2 에 있다.

**출처가 노선마다 다르다.** 그 문서가 노선별 하위 문서로 나뉘어 있고, 신분당선과 용인경전철만 상위 문서의 절에 있다. 그래서 어느 문서의 몇 번째 판에서 옮겼는지가 노선마다 갈린다.

| 노선 | 문서 | 판 | 역 | 문 |
|---|---|---|---|---|
| 신분당선 | 수도권 전철 환승 정보 | r697 | 8 | 18 |
| 2호선 | 〃 /서울 지하철 2호선 | r96 | 21 | 52 |
| 9호선 | 〃 /서울 지하철 9호선 | r75 | 13 | 33 |
| 수인분당선 | 〃 /수도권 전철 수인분당선 | r41 | 18 | 51 |
| 용인경전철 | 수도권 전철 환승 정보 | r697 | 1 | 1 |

`src/main/resources/transfer/<노선>-doors.csv` 가 그 데이터다. **출처는 파일 머리의 주석에 `# source-document:` 형태로 적고 파서가 읽는다.** 코드에 두면 노선마다 달라 관리가 흩어지고, 데이터와 떨어져 있으면 한쪽만 고쳐진다. 하나라도 빠지면 기동을 실패시킨다 - 표기 없이 데이터만 나가는 것이 라이선스 위반이므로 배포 전에 알아야 한다.

역은 노선 순서대로 적는다. 그 순서가 곧 화면 순서라 어긋나면 기동을 실패시킨다 - 코드에서 정렬해 버리면 파일이 흐트러져도 조용히 바로잡혀 사람이 모른다.

**환승 대상이 우리가 담은 노선이면 그 역이 상대 노선에도 있는지 확인한다.** 두 노선이 실제로 그 역에서 만나는지를 보는 것이라, 역명도 노선도 각각은 멀쩡한데 짝이 틀린 행을 잡는다. 원본에서 표를 옮길 때 역 제목을 놓치면 그 표가 앞 역에 붙는데, 다른 검사로는 그것이 드러나지 않는다.

**화면은 갈아탈 노선마다 그 노선의 색 동그라미를 붙인다.** 이름만 늘어놓으면 어느 노선인지 한눈에 안 들어온다. 우리가 담지 않은 노선까지 필요하므로 색은 `lines.yml` 의 `transfer-colors` 에 따로 둔다. **그 맵의 키는 대괄호로 감싸야 한다** - 그러지 않으면 Spring 의 relaxed binding 이 키에서 한글을 떼어내 `1호선` 과 `인천1호선` 이 둘 다 `1` 로 뭉개지고, 나중에 적은 색이 앞의 것을 덮는다.

**시종착역은 축이 다르다.** 신분당선 신사가 그렇고 수인분당선 청량리와 인천도 그렇다. 노선의 끝이라 열차 방면을 가릴 것이 없고, 대신 갈아탈 노선의 방면으로 나뉘거나 아예 방면 구분이 없다. 데이터 모델에서 두 방면을 각각 null 일 수 있게 둔 것이 이 때문이다.

### 2.3.1 국가철도공단 데이터를 쓰지 않는 이유

**축이 다르고, 그 축으로는 내릴 문을 정할 수 없다.** 공식 데이터는 갈아탈 노선의 방면으로만 나누고 지금 탄 열차의 방향을 구분하지 않는다. 그래서 한 방향에 답이 둘이 된다 — 양재에서 3호선 구파발 방면으로 갈아타는 행이 `6-4` 와 `1-1` 두 개다. 나무위키는 같은 두 값을 신사행 `1-1`, 광교행 `6-4` 로 갈라 놓아 타고 있는 열차 기준으로 한 곳이 정해진다.

**이것은 신분당선판의 결함이 아니라 데이터셋 전체의 스펙이다.** 노선을 늘릴 때 노선별로 다시 조사했고 결과가 같았다.

| 노선판 | 행 | 역 | (역, 환승선, 기점) 조합 | 답이 둘 이상 |
|---|---|---|---|---|
| 2호선 | 98 | 21 | 51 | 45 |
| 9호선 | 34 | 8 | 10 | 10 |
| 수인분당선 | 50 | 15 | 30 | 20 |

9호선판은 `환승이후역명` 과 `환승기점역명` 이 아예 비어 있어 방향 구분 자체가 없다. **그러므로 다른 노선을 붙일 때 이 조사를 되풀이할 필요는 없다.** 커버리지가 넉넉한 노선판이 있더라도 축이 맞지 않아 쓸 수 없다.

**신분당선판은 커버리지도 절반이다.** 여덟 역 중 넷만 담는다. 아래는 그 전수 조사다.

신분당선판(공공데이터포털 15041072)은 12행이고 cp949 CSV 다.

```
철도운영기관명,노선명,역명,환승철도운영기관,환승선명,환승이후역명,환승기점역명,차량순서,차량출입문번호
네오트랜스주식회사,신분당선,강남,서울교통공사,2호선,교대(법원.검찰청),충정로(경기대입구),1,1
네오트랜스주식회사,신분당선,정자,코레일,수인분당,미금(분당서울대병원),왕십리,2,4
```

`환승기점역명` 이 갈아탈 노선의 방면을 가른다. 지금 탄 열차가 어느 쪽으로 가는지를 담는 컬럼은 없다.

같은 형식의 데이터셋이 노선별로 나뉘어 있고 수도권만 25건이다. 신분당선 환승에 걸리는 것이 이 정도다.

| 데이터셋 | 공공데이터포털 번호 |
|---|---|
| 신분당선 | 15041072 |
| 수인분당선 | 15041060 |
| 경강선 | 15041066 |
| 에버라인 | 15041075 |
| 서울교통공사 | 15041087 |
| 수도권 1~9호선 | 15041051 ~ 15041059 |
| 서울9호선 | 15041088 |
| 경의중앙선 / 공항철도 | 15041064 / 15041090 |

**이 데이터셋들 안에서는 구멍을 메울 방법이 없다.** 신분당선판은 네오트랜스 관할 구간만 담아 강남, 양재, 정자, 판교 넷뿐이다. 상대 노선 데이터에 역방향이 들어 있는 경우가 있어 3호선판에서 양재, 수인분당선판에서 정자를 얻지만, 그마저 같은 네 역의 반대편일 뿐이다. **신사, 논현, 신논현, 미금은 양쪽 어디에도 없다.** 7호선판에 논현역 행이 0건, 서울9호선판에 신논현역 행이 0건, 수인분당선판에 미금역 행이 0건이다. 새서울철도와 경기철도 구간이 통째로 빠져 있다.

**에버라인판은 행이 있어도 쓸 수 없다.** 기흥역 2행이 전부인데다 컬럼이 7개뿐이라 `차량순서` 와 `차량출입문번호` 가 아예 없다. 데이터셋마다 컬럼 구성이 다르므로 같은 파서로 읽으려면 헤더를 보고 갈라야 한다.

다운로드 URL 의 `atchFileId` 는 데이터 갱신 시 바뀌므로 하드코딩할 수 없다. 데이터셋 페이지(`/data/{번호}/fileData.do`)에서 `fileDownload.do?atchFileId=...&fileDetailSn=...` 를 긁어야 한다.

다른 노선을 붙일 때는 이 조사를 다시 한다. 노선마다 커버리지가 다르므로 공식 데이터가 여덟 역을 다 담는 노선이 있을 수 있다.

### 2.3.2 나무위키 라이선스

나무위키 문서는 **CC BY-NC-SA 2.0 KR** 이고 이 사이트는 비영리이므로 조건을 지키면 쓸 수 있다.

| 조건 | 지키는 방법 |
|---|---|
| BY 저작자표시 | 그 데이터를 쓴 화면에 문서명과 판과 라이선스 링크를 표시 |
| NC 비영리 | 광고, 유료 기능, 제휴 링크를 넣지 않는다 |
| SA 동일조건변경허락 | 데이터 파일에 같은 라이선스를 명시 |

**문서명만으로는 부족해 판까지 적는다.** 위키는 계속 고쳐지므로 문서명은 우리가 옮긴 시점의 내용을 가리키지 못한다. 판 번호는 문서 본문에 없고 활동 기록(`/activity/<문서명>`)의 맨 위 항목에서 읽는다.

**SA 가 코드까지 번지지 않게 파일을 분리한다.** 환승 데이터는 `src/main/resources/transfer/` 아래에만 두고 그 파일들에만 CC BY-NC-SA 2.0 KR 을 건다. 편성 차수도 같은 출처이므로 `lines.yml` 의 그 항목에 문서와 판을 적는다. CC 는 단순히 함께 묶인 것(Collection)을 파생물로 보지 않으므로, 코드는 별도 라이선스를 유지할 수 있다. 섞어 두면 어디까지가 SA 대상인지 나중에 가릴 수 없다.

BY 조건은 화면에서 뺄 수 없으므로 API 응답이 출처를 함께 실어 보내고, 화면은 노선마다 그것을 적는다. 스모크 테스트가 문서명과 판과 라이선스가 모두 살아 있는지 확인한다.

**표를 옮겨 적는 것으로 끝낸다.** 다섯 노선 61역 155개 문이고 문서 다섯 개를 한 번씩 읽었다. 주기적으로 긁어오는 장치를 붙이면 양이 늘어 데이터베이스제작자의 권리(저작권법 93조)가 말하는 "상당한 부분" 에 가까워지고, 문서 구조가 바뀔 때마다 깨진다.

### 2.4 용인경전철

공식 실시간 페이지가 쓰는 엔드포인트를 그대로 쓴다.

```
https://everlinecu.com/api/api009.json
```

응답은 `data[]` 배열이고 항목마다 `StCode`(현재 정거장), `DestCode`(목적지), `TrainNo`, `StatusCode`, `updownCode`, `time`, `LineNo` 를 담는다. 인증이 없고 호출 제한도 공지되어 있지 않다.

역 코드는 `Y110`(기흥)부터 `Y124`(전대에버랜드)까지 순서대로 붙는다. 공식 페이지 스크립트의 역명 배열과 자리를 맞추면 이렇다.

```
Y110 기흥        Y111 강남대      Y112 지석        Y113 어정        Y114 동백
Y115 초당        Y116 삼가        Y117 시청·용인대  Y118 명지대      Y119 김량장
Y120 용인중앙시장 Y121 고진        Y122 보평        Y123 둔전        Y124 전대·에버랜드
```

**`Y109` 는 차량기지이고 실제 역이 아니다.** 공식 스크립트도 이 값을 걸러내므로 우리도 제외한다.

`StatusCode` 는 공식 스크립트 상수와 같다. `3` 이 주행, `2` 가 정차다. `time` 은 그 구간에 들어선 뒤 흐른 초다. 같은 스크립트가 역간 소요시간을 초 단위 배열로 들고 있어, 이 둘을 합치면 역과 역 사이 어디쯤인지 보간할 수 있다.

```
하행: 89 74 78 83 121 147 79 77 64 71 102 110 77 179
상행: 96 82 77 86 122 172 79 75 62 70 76 124 85 184
```

2026-09-21 10:22 기준 10대가 운행 중이었고 목적지는 `Y110` 5대와 `Y124` 5대로 갈렸다.

## 3. 호출 예산

활용사례 등록 전까지는 아래를 지킨다.

| 항목 | 값 |
|---|---|
| 일일 한도 | 1,000회 |
| 1회 최대 반환 | 1,000건 |
| 운행시간 19시간 기준 최소 캐시 주기 | 노선 하나에 70초 |

역 단위로 조회하면 역 개수만큼 곱해지므로, 노선 단위 조회(`realtimePosition`)로 한 번 받아 서버에서 역별로 쪼개는 쪽이 예산에 유리하다.

**예산은 노선 수만큼 나뉜다.** 70초 주기는 노선이 하나일 때의 값이다. 네 노선을 한꺼번에 폴링하면 하루 3,900회가 되어 한도의 네 배다. 그래서 캐시 주기를 늘리는 대신 **보고 있는 노선만 부른다** - 화면은 노선을 접었다 펼치고 펼친 것만 폴링하며, 첫 방문은 한 노선만 펼친 채로 시작한다. 펼친 상태는 그 브라우저에 저장된다.

서버는 실제로 나간 호출 수를 **날짜별로** 세어 응답에 실어 보내고 화면 맨 위에 보인다. 자정에 한도가 돌아오므로 누적이 아니라 그 날 것이어야 하고, 재배포로 0 이 되지 않도록 파일에 남긴다(1.3). 이 값으로 막지는 않는다 - 막아서 화면이 비는 것보다 남은 예산을 보여주고 사람이 판단하는 편이 낫다.

**용인경전철은 이 예산에 들지 않는다.** 서울시 API 가 아니라 공식 실시간 페이지의 엔드포인트를 직접 부르고, 인증도 호출 제한도 없다(2.4).

활용사례 갤러리에 등록하면 이 한도가 풀린다. 그때는 위 제약이 통째로 사라지므로 노선을 다 펼쳐 두고 주기도 줄일 수 있다.

## 4. 스택

ledger-memo 구성을 따르되 저장소 계층을 뺀다.

| 구분 | 사용 |
|---|---|
| 백엔드 | Kotlin, Spring Boot |
| 저장소 | 없음 (기동 시 파싱해 메모리 보관) |
| 런타임 | GraalVM native image (aarch64) |
| 프론트 | 빌드 없는 단일 HTML, vanilla JS, PWA |
| 배포 | GitHub Actions (ARM64 runner), GHCR, Oracle Cloud A1, Podman |
| 앞단 | Apache httpd 리버스 프록시 |

시각표 PDF 파싱에 `pdftotext` 가 필요하다. 런타임 이미지에 poppler 를 넣을지, 빌드 시점에 파싱해 JSON 을 산출물에 포함할지는 정하지 않았다. 후자가 런타임 의존을 없애지만 시각표 개정 때마다 재배포가 필요하다.

### 4.1 native image 로 넘어가기 전에 처리할 것

**Kotlin 의 빈 컬렉션 싱글톤이 native 에서 Jackson 직렬화를 깨뜨린다.** `mapNotNull` 이나 `toList()` 는 결과가 비면 `kotlin.collections.EmptyList` 를 돌려주는데, 이는 Kotlin 내부 object 라 native image 에 메타데이터가 없으면 `KotlinReflectionInternalError: Unresolved class` 로 응답 직렬화가 실패한다.

**이 프로젝트는 매일 밤 그 상태가 된다.** 운행이 끝나면 열차 목록이 비고, 그 빈 목록이 그대로 응답에 실린다. 조치는 두 가지를 함께 한다.

1. `RuntimeHintsRegistrar` 로 `kotlin.collections.EmptyList`, `EmptyMap`, `EmptySet` 을 등록한다.
2. 응답 DTO 에 담기 전에 `ArrayList(...)` 로 감싸 애초에 만들지 않는다.

**JVM 테스트로는 잡히지 않는다.** JVM 에는 메타데이터가 그대로 있어 전부 통과하고 native 바이너리에서만 터진다. 그래서 CI 의 native job 은 빌드만 하지 말고 바이너리를 실제로 띄워 **열차가 0대인 응답까지** 왕복해야 한다.

## 5. 확인 예정

1. 시각표 PDF 파싱을 빌드 시점에 할지 런타임에 할지. 4장 참고.
2. **2호선 `updnLine` 의 0 과 1 이 내선과 외선 중 어느 쪽인지.** 지금은 0 을 내선으로 두었으나 데이터로 확인하지 않았다. 선형 노선처럼 종착역으로 검증할 수 없어, 열차 하나를 시간차로 추적해 역 순서가 어느 쪽으로 움직이는지 봐야 한다. 틀렸다면 좌우가 통째로 뒤집혀 있다.
3. **단축 운행 판정을 노선 끝 기준으로 두는 것이 수인분당선에 맞지 않는다.** 이 노선은 청량리까지 가는 열차가 드물어 왕십리행이 사실상 기본인데, 노선 끝이 아니라는 이유로 거의 모든 열차가 단축으로 표시된다. 표시가 흔해지면 강조하는 뜻이 없어진다. 노선마다 "이건 단축이 아니다" 할 종착역 집합을 따로 둘지 정해야 한다.

## 6. 스코프 밖

- 경로 탐색과 소요시간 계산. 기존 지도 앱이 충분히 잘한다.
- 운임 계산. 신분당선 별도 운임이 있으나 교통카드가 알아서 한다.
- 버스 연계.
- 로그인과 개인화 서버 저장.
